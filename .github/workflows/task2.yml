name: Task 2

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "building..."
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "testing..."
  package:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - run: echo "packaging..."
